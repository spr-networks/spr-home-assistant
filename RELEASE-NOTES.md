# Release Notes

## v0.1.0

- Initial release: ha_sync SPR plugin + Home Assistant custom integration.
- Device tracking (wifi + wired + sprbus push), WAN telemetry sensors,
  guest wifi and per-device blocking switches, restart button, WoL,
  update entity, zeroconf discovery, reproducible container build.
